export const enum PageIds {
    Disclaimer,
    SignIn,
    SignUp,
    SignUpContinue,
    PasswordRecovery,
    Rules
}

export const enum ButtonType {
    Default,
    Transparent,
    TransparentCheckbox,
    Arrow
}

export const enum Rules {
    NoRead,
    Read
}
