export enum PageIds {
    Disclaimer,
    SignIn,
    SignUp,
    SignUpContinue,
    PasswordRecovery,
    Rules
}

export enum ButtonType {
    White,
    Dark
}
export enum Rules {
    NoRead,
    Read
}
